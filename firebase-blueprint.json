{
  "entities": {
    "SharedDoc": {
      "title": "SharedDoc",
      "description": "User generated screenplay or note for cloud delivery",
      "type": "object",
      "properties": {
        "filename": {
          "type": "string",
          "description": "Name of the file"
        },
        "content": {
          "type": "string",
          "description": "Standard base64 payload or raw text content of the file"
        },
        "isBase64": {
          "type": "boolean",
          "description": "Boolean representing if content is base64 encoded"
        },
        "contentType": {
          "type": "string",
          "description": "MIME type of the file"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of creation"
        }
      },
      "required": ["filename", "content", "isBase64", "contentType", "createdAt"]
    }
  },
  "firestore": {
    "/shared_docs/{docId}": {
      "schema": "SharedDoc",
      "description": "Exposed cloud delivery link cache for native WebView compatibility"
    }
  }
}
